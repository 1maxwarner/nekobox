- fix: route AmneziaWG userspace UDP sessions through the virtual AWG address with an ephemeral local port
- fix: prevent Discord voice UDP flows from colliding with `port is in use` and hanging at RTC Connecting
- test: add an AmneziaWG netstack regression test for IPv4 and IPv6 UDP source binding
